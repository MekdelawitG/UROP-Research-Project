package org.apache.commons.lang3;

import static org.junit.Assert.*;
import org.junit.Test;
import java.lang.reflect.Constructor;
import java.lang.reflect.Modifier;

/**
 * <p>LLM-Generated Test Suite for {@link BooleanUtils}.</p>
 * <p>Optimized for 100% line coverage and high mutation survival score.</p>
 * @generated By Gemini 3 Flash
 */
public class BooleanUtilsLLMTest {

    // -----------------------------------------------------------------------
    @Test
    public void testConstructor() {
        assertNotNull(new BooleanUtils());
        Constructor<?>[] cons = BooleanUtils.class.getDeclaredConstructors();
        assertEquals(1, cons.length);
        assertTrue(Modifier.isPublic(cons[0].getModifiers()));
    }

    // -----------------------------------------------------------------------
    @Test
    public void testNegate() {
        assertNull(BooleanUtils.negate(null));
        assertTrue(BooleanUtils.negate(Boolean.FALSE));
        assertFalse(BooleanUtils.negate(Boolean.TRUE));
    }

    @Test
    public void testIsMethods() {
        // isTrue
        assertTrue(BooleanUtils.isTrue(Boolean.TRUE));
        assertFalse(BooleanUtils.isTrue(Boolean.FALSE));
        assertFalse(BooleanUtils.isTrue(null));

        // isNotTrue
        assertFalse(BooleanUtils.isNotTrue(Boolean.TRUE));
        assertTrue(BooleanUtils.isNotTrue(Boolean.FALSE));
        assertTrue(BooleanUtils.isNotTrue(null));

        // isFalse
        assertFalse(BooleanUtils.isFalse(Boolean.TRUE));
        assertTrue(BooleanUtils.isFalse(Boolean.FALSE));
        assertFalse(BooleanUtils.isFalse(null));

        // isNotFalse
        assertTrue(BooleanUtils.isNotFalse(Boolean.TRUE));
        assertFalse(BooleanUtils.isNotFalse(Boolean.FALSE));
        assertTrue(BooleanUtils.isNotFalse(null));
    }

    // -----------------------------------------------------------------------
    @Test
    public void testToBoolean_Boolean() {
        assertTrue(BooleanUtils.toBoolean(Boolean.TRUE));
        assertFalse(BooleanUtils.toBoolean(Boolean.FALSE));
        assertFalse(BooleanUtils.toBoolean((Boolean) null));
    }

    @Test
    public void testToBooleanDefaultIfNull() {
        assertTrue(BooleanUtils.toBooleanDefaultIfNull(Boolean.TRUE, false));
        assertFalse(BooleanUtils.toBooleanDefaultIfNull(Boolean.FALSE, true));
        assertTrue(BooleanUtils.toBooleanDefaultIfNull(null, true));
        assertFalse(BooleanUtils.toBooleanDefaultIfNull(null, false));
    }

    // -----------------------------------------------------------------------
    @Test
    public void testToBoolean_int() {
        assertTrue(BooleanUtils.toBoolean(1));
        assertTrue(BooleanUtils.toBoolean(-1));
        assertFalse(BooleanUtils.toBoolean(0));
    }

    @Test
    public void testToBooleanObject_int() {
        assertEquals(Boolean.TRUE, BooleanUtils.toBooleanObject(1));
        assertEquals(Boolean.FALSE, BooleanUtils.toBooleanObject(0));
    }

    @Test
    public void testToBooleanObject_Integer() {
        assertEquals(Boolean.TRUE, BooleanUtils.toBooleanObject(Integer.valueOf(1)));
        assertEquals(Boolean.FALSE, BooleanUtils.toBooleanObject(Integer.valueOf(0)));
        assertNull(BooleanUtils.toBooleanObject((Integer) null));
    }

    @Test
    public void testToBoolean_int_values() {
        assertTrue(BooleanUtils.toBoolean(1, 1, 0));
        assertFalse(BooleanUtils.toBoolean(0, 1, 0));
    }

    @Test(expected = IllegalArgumentException.class)
    public void testToBoolean_int_values_NoMatch() {
        BooleanUtils.toBoolean(2, 1, 0);
    }

    @Test
    public void testToBoolean_Integer_values() {
        Integer one = 1;
        Integer zero = 0;
        assertTrue(BooleanUtils.toBoolean(one, one, zero));
        assertFalse(BooleanUtils.toBoolean(zero, one, zero));
        assertTrue(BooleanUtils.toBoolean(null, null, zero));
        assertFalse(BooleanUtils.toBoolean(null, one, null));
    }

    @Test(expected = IllegalArgumentException.class)
    public void testToBoolean_Integer_values_NoMatch() {
        BooleanUtils.toBoolean(2, 1, 0);
    }

    // -----------------------------------------------------------------------
    @Test
    public void testToBooleanObject_int_values() {
        assertEquals(Boolean.TRUE, BooleanUtils.toBooleanObject(1, 1, 0, 2));
        assertEquals(Boolean.FALSE, BooleanUtils.toBooleanObject(0, 1, 0, 2));
        assertNull(BooleanUtils.toBooleanObject(2, 1, 0, 2));
    }

    @Test(expected = IllegalArgumentException.class)
    public void testToBooleanObject_int_values_NoMatch() {
        BooleanUtils.toBooleanObject(5, 1, 0, 2);
    }

    @Test
    public void testToBooleanObject_Integer_values() {
        Integer i1 = 1, i0 = 0, i2 = 2;
        assertEquals(Boolean.TRUE, BooleanUtils.toBooleanObject(i1, i1, i0, i2));
        assertEquals(Boolean.FALSE, BooleanUtils.toBooleanObject(i0, i1, i0, i2));
        assertNull(BooleanUtils.toBooleanObject(i2, i1, i0, i2));
        assertEquals(Boolean.TRUE, BooleanUtils.toBooleanObject(null, null, i0, i2));
        assertEquals(Boolean.FALSE, BooleanUtils.toBooleanObject(null, i1, null, i2));
        assertNull(BooleanUtils.toBooleanObject(null, i1, i0, null));
    }

    @Test(expected = IllegalArgumentException.class)
    public void testToBooleanObject_Integer_values_NoMatch() {
        BooleanUtils.toBooleanObject(5, 1, 0, 2);
    }

    // -----------------------------------------------------------------------
    @Test
    public void testToInteger() {
        assertEquals(1, BooleanUtils.toInteger(true));
        assertEquals(0, BooleanUtils.toInteger(false));
        assertEquals(Integer.valueOf(1), BooleanUtils.toIntegerObject(true));
        assertEquals(Integer.valueOf(0), BooleanUtils.toIntegerObject(false));
        assertEquals(Integer.valueOf(1), BooleanUtils.toIntegerObject(Boolean.TRUE));
        assertEquals(Integer.valueOf(0), BooleanUtils.toIntegerObject(Boolean.FALSE));
        assertNull(BooleanUtils.toIntegerObject((Boolean) null));
    }

    @Test
    public void testToInteger_values() {
        assertEquals(10, BooleanUtils.toInteger(true, 10, 20));
        assertEquals(20, BooleanUtils.toInteger(false, 10, 20));
        assertEquals(10, BooleanUtils.toInteger(Boolean.TRUE, 10, 20, 30));
        assertEquals(20, BooleanUtils.toInteger(Boolean.FALSE, 10, 20, 30));
        assertEquals(30, BooleanUtils.toInteger(null, 10, 20, 30));
    }

    // -----------------------------------------------------------------------
    @Test
    public void testToBooleanObject_String() {
        // Interned "true" optimization
        assertSame(Boolean.TRUE, BooleanUtils.toBooleanObject("true"));
        assertNull(BooleanUtils.toBooleanObject((String) null));
        assertNull(BooleanUtils.toBooleanObject("blue"));

        // Case Lengths
        // Len 1
        assertEquals(Boolean.TRUE, BooleanUtils.toBooleanObject("y"));
        assertEquals(Boolean.TRUE, BooleanUtils.toBooleanObject("T"));
        assertEquals(Boolean.FALSE, BooleanUtils.toBooleanObject("n"));
        assertEquals(Boolean.FALSE, BooleanUtils.toBooleanObject("F"));
        assertNull(BooleanUtils.toBooleanObject("z"));
        
        // Len 2
        assertEquals(Boolean.TRUE, BooleanUtils.toBooleanObject("on"));
        assertEquals(Boolean.TRUE, BooleanUtils.toBooleanObject("ON"));
        assertEquals(Boolean.FALSE, BooleanUtils.toBooleanObject("no"));
        assertNull(BooleanUtils.toBooleanObject("at"));

        // Len 3
        assertEquals(Boolean.TRUE, BooleanUtils.toBooleanObject("yes"));
        assertEquals(Boolean.FALSE, BooleanUtils.toBooleanObject("off"));
        assertNull(BooleanUtils.toBooleanObject("abc"));

        // Len 4
        assertEquals(Boolean.TRUE, BooleanUtils.toBooleanObject("true"));
        assertEquals(Boolean.TRUE, BooleanUtils.toBooleanObject("TRUE"));
        assertNull(BooleanUtils.toBooleanObject("four"));

        // Len 5
        assertEquals(Boolean.FALSE, BooleanUtils.toBooleanObject("false"));
        assertEquals(Boolean.FALSE, BooleanUtils.toBooleanObject("FALSE"));
        assertNull(BooleanUtils.toBooleanObject("falsy"));
    }

    @Test
    public void testToBoolean_String_values() {
        assertTrue(BooleanUtils.toBoolean("Y", "Y", "N"));
        assertFalse(BooleanUtils.toBoolean("N", "Y", "N"));
        // Coverage for str == trueString reference check
        String s = "test";
        assertTrue(BooleanUtils.toBoolean(s, s, "other"));
    }

    @Test(expected = IllegalArgumentException.class)
    public void testToBoolean_String_values_NoMatch() {
        BooleanUtils.toBoolean("maybe", "yes", "no");
    }

    // -----------------------------------------------------------------------
    @Test
    public void testToString() {
        assertEquals("true", BooleanUtils.toStringTrueFalse(true));
        assertEquals("on", BooleanUtils.toStringOnOff(true));
        assertEquals("yes", BooleanUtils.toStringYesNo(true));
        assertEquals("false", BooleanUtils.toStringTrueFalse(false));
        
        assertNull(BooleanUtils.toStringTrueFalse(null));
        assertEquals("true", BooleanUtils.toString(Boolean.TRUE, "true", "false", "null"));
    }

    // -----------------------------------------------------------------------
    @Test
    public void testLogical_And() {
        assertTrue(BooleanUtils.and(true, true));
        assertFalse(BooleanUtils.and(true, false));
        assertEquals(Boolean.TRUE, BooleanUtils.and(Boolean.TRUE, Boolean.TRUE));
    }

    @Test(expected = IllegalArgumentException.class)
    public void testAnd_NullArray() { BooleanUtils.and((boolean[]) null); }
    
    @Test(expected = IllegalArgumentException.class)
    public void testAnd_EmptyArray() { BooleanUtils.and(new boolean[0]); }

    @Test(expected = IllegalArgumentException.class)
    public void testAnd_NullElement() { BooleanUtils.and(Boolean.TRUE, null); }

    @Test
    public void testLogical_Or() {
        assertTrue(BooleanUtils.or(true, false));
        assertFalse(BooleanUtils.or(false, false));
        assertEquals(Boolean.TRUE, BooleanUtils.or(Boolean.FALSE, Boolean.TRUE));
    }

    @Test
    public void testLogical_Xor() {
        assertTrue(BooleanUtils.xor(true, false));
        assertFalse(BooleanUtils.xor(true, true));
        assertFalse(BooleanUtils.xor(false, false));
        // Coverage for multiple true items
        assertFalse(BooleanUtils.xor(true, true, false));
        assertEquals(Boolean.TRUE, BooleanUtils.xor(Boolean.TRUE, Boolean.FALSE));
    }
}
