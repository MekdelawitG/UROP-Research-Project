package org.apache.commons.lang3;

import static org.junit.Assert.*;
import org.junit.Test;
import java.lang.reflect.Constructor;
import java.lang.reflect.Modifier;

/**
 * <p>LLM-Generated Test Suite for {@link CharUtils}.</p>
 * <p>Optimized for 100% line coverage and high mutation score.</p>
 * * @generated By Gemini 3 Flash
 */
public class CharUtilsLLMTest {

    private static final Character CHAR_A = Character.valueOf('A');
    private static final Character CHAR_B = Character.valueOf('B');

    // -----------------------------------------------------------------------
    @Test
    public void testConstructor() {
        // Covers the public constructor for JavaBean tools
        assertNotNull(new CharUtils());
        Constructor<?>[] cons = CharUtils.class.getDeclaredConstructors();
        assertEquals(1, cons.length);
        assertTrue(Modifier.isPublic(cons[0].getModifiers()));
    }

    @Test
    public void testConstants() {
        assertEquals('\n', CharUtils.LF);
        assertEquals('\r', CharUtils.CR);
    }

    // -----------------------------------------------------------------------
    @SuppressWarnings("deprecation")
    @Test
    public void testToCharacterObject_char() {
        assertEquals(Character.valueOf('a'), CharUtils.toCharacterObject('a'));
        // Verify caching (identity)
        assertSame(CharUtils.toCharacterObject('a'), CharUtils.toCharacterObject('a'));
    }

    @Test
    public void testToCharacterObject_String() {
        assertNull(CharUtils.toCharacterObject((String) null));
        assertNull(CharUtils.toCharacterObject(""));
        assertEquals(CHAR_A, CharUtils.toCharacterObject("A"));
        assertEquals(CHAR_A, CharUtils.toCharacterObject("BA")); // Only first char
    }

    // -----------------------------------------------------------------------
    @Test
    public void testToChar_Character() {
        assertEquals('A', CharUtils.toChar(CHAR_A));
    }

    @Test(expected = IllegalArgumentException.class)
    public void testToChar_CharacterNull() {
        CharUtils.toChar((Character) null);
    }

    @Test
    public void testToChar_Character_charDefault() {
        assertEquals('A', CharUtils.toChar(CHAR_A, 'X'));
        assertEquals('X', CharUtils.toChar((Character) null, 'X'));
    }

    @Test
    public void testToChar_String() {
        assertEquals('A', CharUtils.toChar("A"));
        assertEquals('B', CharUtils.toChar("BA"));
    }

    @Test(expected = IllegalArgumentException.class)
    public void testToChar_StringEmpty() {
        CharUtils.toChar("");
    }

    @Test(expected = IllegalArgumentException.class)
    public void testToChar_StringNull() {
        CharUtils.toChar((String) null);
    }

    @Test
    public void testToChar_String_charDefault() {
        assertEquals('A', CharUtils.toChar("A", 'X'));
        assertEquals('X', CharUtils.toChar("", 'X'));
        assertEquals('X', CharUtils.toChar((String) null, 'X'));
    }

    // -----------------------------------------------------------------------
    @Test
    public void testToIntValue_char() {
        assertEquals(0, CharUtils.toIntValue('0'));
        assertEquals(9, CharUtils.toIntValue('9'));
    }

    @Test(expected = IllegalArgumentException.class)
    public void testToIntValue_char_Invalid() {
        CharUtils.toIntValue('a');
    }

    @Test
    public void testToIntValue_char_intDefault() {
        assertEquals(3, CharUtils.toIntValue('3', -1));
        assertEquals(-1, CharUtils.toIntValue('a', -1));
    }

    @Test
    public void testToIntValue_Character() {
        assertEquals(5, CharUtils.toIntValue(Character.valueOf('5')));
    }

    @Test(expected = IllegalArgumentException.class)
    public void testToIntValue_CharacterNull() {
        CharUtils.toIntValue((Character) null);
    }

    @Test
    public void testToIntValue_Character_intDefault() {
        assertEquals(3, CharUtils.toIntValue(Character.valueOf('3'), -1));
        assertEquals(-1, CharUtils.toIntValue(Character.valueOf('A'), -1));
        assertEquals(-1, CharUtils.toIntValue((Character) null, -1));
    }

    // -----------------------------------------------------------------------
    @Test
    public void testToString_char() {
        assertEquals("a", CharUtils.toString('a'));
        // Test cache hit
        assertSame(CharUtils.toString('a'), CharUtils.toString('a'));
        // Test cache miss (boundary > 127)
        assertEquals("\u0080", CharUtils.toString((char) 128));
    }

    @Test
    public void testToString_Character() {
        assertNull(CharUtils.toString((Character) null));
        assertEquals("A", CharUtils.toString(CHAR_A));
    }

    // -----------------------------------------------------------------------
    @Test
    public void testUnicodeEscaped_char() {
        // Test all four branch conditions in unicodeEscaped logic
        assertEquals("\\u0005", CharUtils.unicodeEscaped((char) 0x5));
        assertEquals("\\u0015", CharUtils.unicodeEscaped((char) 0x15));
        assertEquals("\\u0115", CharUtils.unicodeEscaped((char) 0x115));
        assertEquals("\\u1115", CharUtils.unicodeEscaped((char) 0x1115));
    }

    @Test
    public void testUnicodeEscaped_Character() {
        assertNull(CharUtils.unicodeEscaped((Character) null));
        assertEquals("\\u0041", CharUtils.unicodeEscaped(CHAR_A));
    }

    // -----------------------------------------------------------------------
    @Test
    public void testIsAscii() {
        assertTrue(CharUtils.isAscii('a'));
        assertTrue(CharUtils.isAscii((char) 127));
        assertFalse(CharUtils.isAscii((char) 128));
    }

    @Test
    public void testIsAsciiPrintable() {
        assertFalse(CharUtils.isAsciiPrintable((char) 31));
        assertTrue(CharUtils.isAsciiPrintable((char) 32));
        assertTrue(CharUtils.isAsciiPrintable((char) 126));
        assertFalse(CharUtils.isAsciiPrintable((char) 127));
    }

    @Test
    public void testIsAsciiControl() {
        assertTrue(CharUtils.isAsciiControl((char) 31));
        assertFalse(CharUtils.isAsciiControl((char) 32));
        assertFalse(CharUtils.isAsciiControl((char) 126));
        assertTrue(CharUtils.isAsciiControl((char) 127));
    }

    @Test
    public void testIsAsciiAlpha() {
        assertTrue(CharUtils.isAsciiAlpha('A'));
        assertTrue(CharUtils.isAsciiAlpha('Z'));
        assertTrue(CharUtils.isAsciiAlpha('a'));
        assertTrue(CharUtils.isAsciiAlpha('z'));
        assertFalse(CharUtils.isAsciiAlpha('1'));
        assertFalse(CharUtils.isAsciiAlpha((char)('A' - 1)));
        assertFalse(CharUtils.isAsciiAlpha((char)('Z' + 1)));
    }

    @Test
    public void testIsAsciiAlphaUpper() {
        assertTrue(CharUtils.isAsciiAlphaUpper('A'));
        assertTrue(CharUtils.isAsciiAlphaUpper('Z'));
        assertFalse(CharUtils.isAsciiAlphaUpper('a'));
        assertFalse(CharUtils.isAsciiAlphaUpper((char)('A' - 1)));
        assertFalse(CharUtils.isAsciiAlphaUpper((char)('Z' + 1)));
    }

    @Test
    public void testIsAsciiAlphaLower() {
        assertTrue(CharUtils.isAsciiAlphaLower('a'));
        assertTrue(CharUtils.isAsciiAlphaLower('z'));
        assertFalse(CharUtils.isAsciiAlphaLower('A'));
        assertFalse(CharUtils.isAsciiAlphaLower((char)('a' - 1)));
        assertFalse(CharUtils.isAsciiAlphaLower((char)('z' + 1)));
    }

    @Test
    public void testIsAsciiNumeric() {
        assertTrue(CharUtils.isAsciiNumeric('0'));
        assertTrue(CharUtils.isAsciiNumeric('9'));
        assertFalse(CharUtils.isAsciiNumeric((char)('0' - 1)));
        assertFalse(CharUtils.isAsciiNumeric((char)('9' + 1)));
    }

    @Test
    public void testIsAsciiAlphanumeric() {
        assertTrue(CharUtils.isAsciiAlphanumeric('A'));
        assertTrue(CharUtils.isAsciiAlphanumeric('0'));
        assertTrue(CharUtils.isAsciiAlphanumeric('z'));
        assertFalse(CharUtils.isAsciiAlphanumeric('-'));
    }
}
